mutation UpdateMe(
  $firstName: String
  $lastName: String
  $address: AddressInput
) {
  updateMe(firstName: $firstName, lastName: $lastName, address: $address) {
    id
    firstName
    lastName
    address {
      name
      line1
      line2
      postalCode
      city
      country
    }
  }
}
