mutation CreateCheckoutSession(
  $offerId: ID!
  $customPrice: Int
  $metadata: JSON
  $returnUrl: String
) {
  createCheckoutSession(
    offerId: $offerId
    options: {
      uiMode: EMBEDDED
      customPrice: $customPrice
      metadata: $metadata
      returnURL: $returnUrl
    }
  ) {
    company
    sessionId
    clientSecret
    url
  }
}
