query GetOffer($offerId: ID!, $promoCode: String) {
  offer: getOffer(offerId: $offerId, promoCode: $promoCode) {
    id
    company
    name

    price {
      amount
      currency
      recurring {
        interval
      }
    }

    customPrice {
      min
      max
      step
    }

    discount {
      name
      amountOff
    }
  }
}
